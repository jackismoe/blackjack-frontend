const appReducer = (state = {player: []}, action) => {
  switch (action.type) {
    case 'CREATE_PLAYER':
      debugger
    // eslint-disable-next-line
    default:
      return state
  }
}

export default appReducer